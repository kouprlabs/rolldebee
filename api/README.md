# Rolldebee API

## Getting Started

Install [klint](https://ktlint.github.io).

For macOS:

```shell
brew install ktlint
```

Format code:

```shell
ktlint --format
```

Run:

```shell
./gradlew bootRun
```

Build Docker image:

```shell
docker build -t rolldebee/api .
```
