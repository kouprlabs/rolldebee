# Rolldebee API

Install [klint](https://ktlint.github.io).

Format code:

```shell
ktlint --format
```

Run for development:

```shell
./gradlew bootRun
```

Build Docker image:

```shell
docker build -t rolldebee/api .
```
